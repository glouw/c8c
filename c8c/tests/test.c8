sprite one = { 0x1, 0x2, 0x3, 0x4, 10 };

sprite two = {
    0x1,
    0x2,
    0x3,
};

void ternary()
{
    byte c = 1 + 3 ? 1 + 2 + 1 : (1 - 1 - 1);
}

void sums()
{
    byte a = 1 + 1;
    byte b = ++a;
    byte c = a++;
}

void negs()
{
    byte a = 1 + 1;
    byte b = --a;
    byte c = a--;
}

void main()
{
    byte d = 0;
}
