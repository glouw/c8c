#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <stdint.h>

int main(void)
{
    uint8_t v[1] = { 123 };
    uint8_t rom[3];
    int x = 0;
    printf("%d, %d, %d\n", rom[0], rom[1], rom[2]);
}
